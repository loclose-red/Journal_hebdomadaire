{% extends 'base.html.twig' %}

{% block title %}Edit Journaliste{% endblock %}

{% block body %}
    <h1>Edit Journaliste</h1>

    {{ include('journaliste/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('journaliste_index') }}">back to list</a>

    {{ include('journaliste/_delete_form.html.twig') }}
{% endblock %}
