<form method="post" action="{{ path('personnalite_delete', {'id': personnalite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personnalite.id) }}">
    <button class="btn">Delete</button>
</form>
